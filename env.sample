# ==================================
# ENVIRONMENT VARIABLES TEMPLATE
# ==================================
# Copy this file and customize for your environment
# DO NOT commit files with real passwords to git!

# Server Configuration
SERVER_HTTP_PORT=8080
SERVER_GRPC_PORT=9090

# Docker Host Ports (where you access from your machine)
HOST_HTTP_PORT=8080
HOST_GRPC_PORT=9090
HOST_POSTGRES_PORT=5432

# Database Configuration (for application runtime)
# WARNING: sslmode=disable is only for local dev - use sslmode=require in production
DATABASE_URL=postgres://user:password@localhost:5432/devices?sslmode=disable

# PostgreSQL Credentials (used by docker-compose AND Makefile)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=devices

# ==================================
# QUICK START
# ==================================
# 1. Copy this file: cp env.sample .env
# 2. Update passwords and configuration
# 3. Load environment:
#    set -a; source .env; set +a
# 4. Start services:
#    docker-compose up -d
# 5. Run migrations:
#    make migrate-up

# ==================================
# MULTIPLE ENVIRONMENTS
# ==================================
# Development:  cp .env.local .env
# Staging:      set -a; source .env.staging; set +a
# Production:   set -a; source .env.production; set +a
# Testing:      set -a; source .env.test; set +a
