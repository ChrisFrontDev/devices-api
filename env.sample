# Server Configuration
SERVER_HTTP_PORT=8080
SERVER_GRPC_PORT=9090

# Database Configuration (for local development - running Go directly on host)
DATABASE_URL=postgres://user:password@localhost:5432/devices?sslmode=disable

# PostgreSQL Credentials (used by docker-compose for both containers)
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=devices
